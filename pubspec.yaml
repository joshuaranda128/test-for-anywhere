name: demo_real_estate_project
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  ## State Management
  get: ^4.6.5

  # API Calls
  dio: ^5.1.1

  ## Functional Programming
  dartz: ^0.10.1
  equatable: ^2.0.5

  ## Data Connection Checker
  connectivity_plus: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
